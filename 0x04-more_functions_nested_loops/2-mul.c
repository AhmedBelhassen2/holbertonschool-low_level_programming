#include "holberton.h"
/**
 * mul - checks if c is digit between 0 and 9 or not
 * @a: first integer
 * @b: second integer
 * Return: n  the multiplication value
 */
int mul(int a, int b)
{
int n;
n = a * b;
return (n);
}
